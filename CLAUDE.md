# 사진

Use Claude Code model `claude-fable-5-1` with `--effort high` when available.
Apply `.claude/skills/impeccable/SKILL.md` to all interface work. Preserve the
existing note/carddav/caldav series: Pretendard, neutral dark surfaces, compact
sidebar, restrained gradient only in the app icon. All user-facing copy is Korean.

Sibling projects in C:/code/note, C:/code/carddav and C:/code/caldav are read-only
references. Never copy their credentials, signing keys, private data or git folders.
NAS originals must never be modified. Viewing sources are read-only; only explicitly
selected backup destinations accept new, non-overwriting files. Private APIs are accessible through embedded
Tailscale only; public photo.3chan.kr serves assets and installer files only.
Keep generated caches and credentials out of git. Test authorization, path boundaries,
index reconciliation and thumbnail generation before deployment.

Requested model probe on 2026-09-13 returned HTTP 429 (Fable usage limit);
no code was produced by Claude in that probe. Do not claim otherwise.
